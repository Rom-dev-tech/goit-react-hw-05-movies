import Navigation from '../Navigation/Navigation';
import '../AppBar/AppBar.scss';

export const AppBar = () => {
  return (
    <header className="header">
      <Navigation />
    </header>
  );
};
